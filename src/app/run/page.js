"use client";

import { Box, Typography } from "@mui/material";

export default function RunPage() {
  return (
    <Box p={2}>
      <Typography variant="h6" gutterBottom>
        실행 관리
      </Typography>
      <Typography>여기에 실행 관리 관련 테이블/기능을 넣으면 됩니다.</Typography>
    </Box>
  );
}
